import { Text, SafeAreaView } from "react-native";
import styles from "../styles";

export default function News() {
    return (
        <SafeAreaView style={styles.container}>
            <Text style={styles.dashboardTitle}>Groups</Text>
        </SafeAreaView>
    );
}
